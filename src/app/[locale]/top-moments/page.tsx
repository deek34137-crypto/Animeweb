import React from 'react';
import TopMomentsClient from './TopMomentsClient';

export const metadata = {
  title: 'Top Moments — Community Anime Scene Rankings | Aniworld',
  description: 'Vote and discover the greatest anime moments and scenes ranked by the community.',
};

export default function TopMomentsPage() {
  return <TopMomentsClient />;
}
