// src/lib/pipeline/versioning.ts
/**
 * Pipeline version information.
 * The version is read from config/pipelineVersion.json at runtime.
 * For now we export a static constant; in production this could be generated
 * during build/deployment.
 */
export const version = 1;
